{
  "builds": [
    {
      "src": "./app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app.js"
    }
  ],
  "scripts": {
    "build": "cd arrowclient && vite build",
    "install": "npm install && cd arrowclient && npm install"
  }
}
